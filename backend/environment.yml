# To update this file with newly installed dependencies `conda env export --from-history > environment.yml`

name: sniff-env
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - fastapi
      - "uvicorn[standard]"
      - motor
      - python-dotenv
